usage: /home/yadu/Research_Work/spirob-mjlab/.venv/bin/train Mjlab-SpiRob-EggToBucket-Stage1 [-h] [OPTIONS]

╭─ options ────────────────────────────────────────────────────────────────────╮
│ -h, --help                                                                   │
│       show this help message and exit                                        │
│ --registry-name {None}|STR                                                   │
│       (default: None)                                                        │
│ --video {True,False}                                                         │
│       (default: False)                                                       │
│ --video-length INT                                                           │
│       (default: 200)                                                         │
│ --video-interval INT                                                         │
│       (default: 2000)                                                        │
│ --enable-nan-guard {True,False}                                              │
│       (default: False)                                                       │
│ --log-root STR                                                               │
│       Root directory under which experiment logs are written. (default:      │
│       logs/rsl_rl)                                                           │
│ --torchrunx-log-dir {None}|STR                                               │
│       (default: None)                                                        │
│ --wandb-run-path {None}|STR                                                  │
│       (default: None)                                                        │
│ --wandb-checkpoint-name {None}|STR                                           │
│       Optional checkpoint name within the W&B run to load (e.g.              │
│       'model_4000.pt'). (default: None)                                      │
│ --gpu-ids {None}|[INT,...]|{all}                                             │
│       (default: '[0]')                                                       │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env options ────────────────────────────────────────────────────────────────╮
│ --env.decimation INT                                                         │
│       Number of physics simulation steps per environment step. Higher values │
│       mean                                                                   │
│       coarser control frequency. Environment step duration = physics_dt *    │
│       decimation. (default: 100)                                             │
│ --env.seed {None}|INT                                                        │
│       Random seed for reproducibility. If None, a random seed is used. The   │
│       actual                                                                 │
│       seed used is stored back into this field after initialization.         │
│       (default: None)                                                        │
│ --env.episode-length-s FLOAT                                                 │
│       Duration of an episode (in seconds).                                   │
│                                                                              │
│                                                                              │
│       Episode length in steps is computed as:                                │
│         ceil(episode_length_s / (sim.mujoco.timestep * decimation))          │
│       (default: 8.0)                                                         │
│ --env.is-finite-horizon {True,False}                                         │
│       Whether the task has a finite or infinite horizon. Defaults to False   │
│       (infinite).                                                            │
│                                                                              │
│                                                                              │
│       - **Finite horizon (True)**: The time limit defines the task boundary. │
│       When reached,                                                          │
│         no future value exists beyond it, so the agent receives a terminal   │
│       done signal.                                                           │
│       - **Infinite horizon (False)**: The time limit is an artificial        │
│       cutoff. The agent                                                      │
│         receives a truncated done signal to bootstrap the value of           │
│       continuing beyond the                                                  │
│         limit. (default: False)                                              │
│ --env.auto-reset {True,False}                                                │
│       Whether to automatically reset environments that terminate or time     │
│       out.                                                                   │
│                                                                              │
│                                                                              │
│       When True (default), ``step()`` resets done environments and returns   │
│       post-reset                                                             │
│       observations. When False, ``step()`` returns the true terminal         │
│       observation and the                                                    │
│       caller must explicitly call ``reset(env_ids=...)`` for done            │
│       environments before the                                                │
│       next ``step()``.                                                       │
│                                                                              │
│                                                                              │
│       Note: mjlab's bundled ``train.py`` goes through rsl_rl's               │
│       ``OnPolicyRunner``, which                                              │
│       does not drive manual resets. ``auto_reset=False`` is intended for     │
│       users running                                                          │
│       their own training loop (or a wrapper that handles the reset between   │
│       steps). (default: True)                                                │
│ --env.scale-rewards-by-dt {True,False}                                       │
│       Whether to multiply rewards by the environment step duration (dt).     │
│                                                                              │
│                                                                              │
│       When True (default), reward values are scaled by step_dt to normalize  │
│       cumulative                                                             │
│       episodic rewards across different simulation frequencies. Set to False │
│       for                                                                    │
│       algorithms that expect unscaled reward signals (e.g., HER, static      │
│       reward scaling). (default: True)                                       │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene options ──────────────────────────────────────────────────────────╮
│ Scene configuration defining terrain, entities, and sensors. The scene       │
│ specifies ``num_envs``, the number of parallel environments.                 │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.scene.num-envs INT                                                     │
│       Number of parallel environments. (default: 64)                         │
│ --env.scene.env-spacing FLOAT                                                │
│       Spacing between environment origins in meters. (default: 0.7)          │
│ --env.scene.extent {None}|FLOAT                                              │
│       Override for ``mjModel.stat.extent``. If ``None``, MuJoCo computes     │
│       it automatically. (default: None)                                      │
│ --env.scene.spec-fn {fixed}                                                  │
│       Optional callback to modify the ``MjSpec`` after entities and sensors  │
│       have been added but before compilation. (fixed to: None)               │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.terrain options ──────────────────────────────────────────────────╮
│ Configuration for terrain as an entity.                                      │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.scene.terrain.spec-fn {fixed}                                          │
│       (fixed to: <function EntityCfg.<lambda>.<locals>.<lambda> at           │
│       0x793076924360>)                                                       │
│ --env.scene.terrain.sort-actuators {True,False}                              │
│       When True, reorder actuators so that ``model.ctrl`` follows            │
│       joint/tendon/site                                                      │
│       definition order rather than the order actuators appear in the config. │
│       XML actuators                                                          │
│       are excluded from sorting and always retain their declaration order.   │
│       (default: False)                                                       │
│ --env.scene.terrain.terrain-type {generator,plane}                           │
│       Type of terrain to generate. "generator" uses procedural terrain with  │
│       sub-terrain grid, "plane" creates a flat ground plane. (default:       │
│       plane)                                                                 │
│ --env.scene.terrain.env-spacing {None}|FLOAT                                 │
│       Distance between environment origins when using grid layout. Required  │
│       for                                                                    │
│       "plane" terrain or when no sub-terrain origins exist. (default: 2.0)   │
│ --env.scene.terrain.max-init-terrain-level {None}|INT                        │
│       Maximum initial difficulty level (row index) for environment placement │
│       in                                                                     │
│       curriculum mode. None uses all available rows. (default: None)         │
│ --env.scene.terrain.num-envs INT                                             │
│       Number of parallel environments to create. This will get overridden by │
│       the                                                                    │
│       scene configuration if specified there. (default: 1)                   │
│ --env.scene.terrain.debug-vis {True,False}                                   │
│       Add visualization sites for environment origins, terrain origins, and  │
│       flat patches. Defaults to False. (default: False)                      │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.terrain.init-state options ───────────────────────────────────────╮
│ --env.scene.terrain.init-state.pos (FLOAT,FLOAT,FLOAT)                       │
│       Root position and orientation. (default: '(0.0, 0.0, 0.0)')            │
│ --env.scene.terrain.init-state.rot (FLOAT,FLOAT,FLOAT,FLOAT)                 │
│       Root position and orientation. (default: '(1.0, 0.0, 0.0, 0.0)')       │
│ --env.scene.terrain.init-state.lin-vel (FLOAT,FLOAT,FLOAT)                   │
│       Root linear and angular velocity (only for floating base entities).    │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.scene.terrain.init-state.ang-vel (FLOAT,FLOAT,FLOAT)                   │
│       Root linear and angular velocity (only for floating base entities).    │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.scene.terrain.init-state.joint-pos {None,STR:FLOAT,...}                │
│       Articulation (only for articulated entities). Set to None to use the   │
│       model's existing keyframe (errors if none exists). (default:           │
│       '{'"'"'.*'"'"': 0.0}')                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.terrain.init-state.joint-vel options ─────────────────────────────╮
│ Articulation (only for articulated entities). Set to None to use the model's │
│ existing keyframe (errors if none exists).                                   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.scene.terrain.init-state.joint-vel.* FLOAT                             │
│       (default: 0.0)                                                         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.terrain.lights.0 options ─────────────────────────────────────────╮
│ --env.scene.terrain.lights.0.name {None}|STR                                 │
│       Name of the light (optional). (default: sun)                           │
│ --env.scene.terrain.lights.0.body STR                                        │
│       Body to attach light to (default: "world"). (default: world)           │
│ --env.scene.terrain.lights.0.mode STR                                        │
│       Light mode ("fixed", "track", "trackcom", "targetbody",                │
│       "targetbodycom"). (default: fixed)                                     │
│ --env.scene.terrain.lights.0.target {None}|STR                               │
│       Target body for tracking modes (optional). (default: None)             │
│ --env.scene.terrain.lights.0.type {spot,directional}                         │
│       Light type ("spot" or "directional"). (default: directional)           │
│ --env.scene.terrain.lights.0.castshadow {True,False}                         │
│       Whether light casts shadows. (default: True)                           │
│ --env.scene.terrain.lights.0.pos (FLOAT,FLOAT,FLOAT)                         │
│       Light position (x, y, z). (default: '(0.0, 0.0, 1.5)')                 │
│ --env.scene.terrain.lights.0.dir (FLOAT,FLOAT,FLOAT)                         │
│       Light direction vector (x, y, z). (default: '(0.0, 0.0, -1.0)')        │
│ --env.scene.terrain.lights.0.cutoff FLOAT                                    │
│       Spot light cutoff angle in degrees. (default: 45.0)                    │
│ --env.scene.terrain.lights.0.exponent FLOAT                                  │
│       Spot light exponent. (default: 10.0)                                   │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.terrain.textures.0 options ───────────────────────────────────────╮
│ --env.scene.terrain.textures.0.name STR                                      │
│       Name of the texture. (default: groundplane)                            │
│ --env.scene.terrain.textures.0.type {2d,cube,skybox}                         │
│       Texture type ("2d", "cube", or "skybox"). (default: 2d)                │
│ --env.scene.terrain.textures.0.builtin {checker,gradient,flat,none}          │
│       Built-in texture pattern ("checker", "gradient", "flat", or "none").   │
│       (default: checker)                                                     │
│ --env.scene.terrain.textures.0.rgb1 (FLOAT,FLOAT,FLOAT)                      │
│       First RGB color tuple. (default: '(0.2, 0.3, 0.4)')                    │
│ --env.scene.terrain.textures.0.rgb2 (FLOAT,FLOAT,FLOAT)                      │
│       Second RGB color tuple. (default: '(0.1, 0.2, 0.3)')                   │
│ --env.scene.terrain.textures.0.width INT                                     │
│       Texture width in pixels (must be positive). (default: 300)             │
│ --env.scene.terrain.textures.0.height INT                                    │
│       Texture height in pixels (must be positive). (default: 300)            │
│ --env.scene.terrain.textures.0.mark {edge,cross,random,none}                 │
│       Marking pattern ("edge", "cross", "random", or "none"). (default:      │
│       edge)                                                                  │
│ --env.scene.terrain.textures.0.markrgb (FLOAT,FLOAT,FLOAT)                   │
│       RGB color for markings. (default: '(0.8, 0.8, 0.8)')                   │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.terrain.materials.0 options ──────────────────────────────────────╮
│ --env.scene.terrain.materials.0.name STR                                     │
│       Name of the material. (default: groundplane)                           │
│ --env.scene.terrain.materials.0.rgba (FLOAT,FLOAT,FLOAT,FLOAT)               │
│       RGBA color of the material. Without a texture this is the direct       │
│       surface color; with a texture it multiplies the texture colors.        │
│       (default: '(1.0, 1.0, 1.0, 1.0)')                                      │
│ --env.scene.terrain.materials.0.texuniform {True,False}                      │
│       Whether texture coordinates are uniform. (default: True)               │
│ --env.scene.terrain.materials.0.texrepeat (FLOAT,FLOAT)                      │
│       Texture repeat pattern (width, height). Must be positive. (default:    │
│       '(4.0, 4.0)')                                                          │
│ --env.scene.terrain.materials.0.reflectance FLOAT                            │
│       Material reflectance value. (default: 0.2)                             │
│ --env.scene.terrain.materials.0.texture {None}|STR                           │
│       Name of texture to apply (optional). (default: groundplane)            │
│ --env.scene.terrain.materials.0.geom-names-expr {None}|(STR,...)             │
│       Regex patterns to match geom names. Matching geoms will have their     │
│       material set to this material. ``None`` means no assignment. (default: │
│       '('"'"'terrain$'"'"',)')                                               │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.robot options ───────────────────────────────────────────╮
│ --env.scene.entities.robot.spec-fn {fixed}                                   │
│       (fixed to: <function spirob_robot_spec at 0x79307717b9c0>)             │
│ --env.scene.entities.robot.sort-actuators {True,False}                       │
│       When True, reorder actuators so that ``model.ctrl`` follows            │
│       joint/tendon/site                                                      │
│       definition order rather than the order actuators appear in the config. │
│       XML actuators                                                          │
│       are excluded from sorting and always retain their declaration order.   │
│       (default: False)                                                       │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.robot.init-state options ────────────────────────────────╮
│ --env.scene.entities.robot.init-state.pos (FLOAT,FLOAT,FLOAT)                │
│       Root position and orientation. (default: '(0.0, 0.0, 0.0)')            │
│ --env.scene.entities.robot.init-state.rot (FLOAT,FLOAT,FLOAT,FLOAT)          │
│       Root position and orientation. (default: '(1.0, 0.0, 0.0, 0.0)')       │
│ --env.scene.entities.robot.init-state.lin-vel (FLOAT,FLOAT,FLOAT)            │
│       Root linear and angular velocity (only for floating base entities).    │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.scene.entities.robot.init-state.ang-vel (FLOAT,FLOAT,FLOAT)            │
│       Root linear and angular velocity (only for floating base entities).    │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.scene.entities.robot.init-state.joint-pos {None,STR:FLOAT,...}         │
│       Articulation (only for articulated entities). Set to None to use the   │
│       model's existing keyframe (errors if none exists). (default:           │
│       '{'"'"'.*'"'"': 0.0}')                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.robot.init-state.joint-vel options ──────────────────────╮
│ Articulation (only for articulated entities). Set to None to use the model's │
│ existing keyframe (errors if none exists).                                   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.scene.entities.robot.init-state.joint-vel.* FLOAT                      │
│       (default: 0.0)                                                         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.robot.articulation options ──────────────────────────────╮
│ --env.scene.entities.robot.articulation.soft-joint-pos-limit-factor FLOAT    │
│       (default: 1.0)                                                         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.robot.articulation.actuators.0 options ──────────────────╮
│ --env.scene.entities.robot.articulation.actuators.0.target-names-expr (STR,  │
│ ...)                                                                         │
│       Targets that are part of this actuator group.                          │
│                                                                              │
│                                                                              │
│       Can be a tuple of names or tuple of regex expressions. Interpreted     │
│       based on                                                               │
│       transmission_type. (default: '('"'"'cable_0'"'"', '"'"'cable_1'"'"')') │
│ --env.scene.entities.robot.articulation.actuators.0.transmission-type        │
│ {JOINT,TENDON,SITE}                                                          │
│       Transmission type. Defaults to JOINT. (default: TENDON)                │
│ --env.scene.entities.robot.articulation.actuators.0.armature {None}|FLOAT    │
│       Reflected rotor inertia. None preserves the XML value. (default: None) │
│ --env.scene.entities.robot.articulation.actuators.0.frictionloss             │
│ {None}|FLOAT                                                                 │
│       Friction loss force limit. None preserves the XML value.               │
│                                                                              │
│                                                                              │
│       Applies a constant friction force opposing motion, independent of load │
│       or velocity.                                                           │
│       Also known as dry friction or load-independent friction. (default:     │
│       None)                                                                  │
│ --env.scene.entities.robot.articulation.actuators.0.viscous-damping          │
│ {None}|FLOAT                                                                 │
│       Passive viscous damping coefficient. None preserves the XML value.     │
│                                                                              │
│                                                                              │
│       Produces a dissipative force f(v) = -b·v proportional to velocity.     │
│       Always present                                                         │
│       regardless of actuator activity. Unlike ``damping`` (the PD derivative │
│       gain kv, which                                                         │
│       is active control), this is a passive property.                        │
│                                                                              │
│                                                                              │
│       Maps to ``<joint damping>`` for JOINT transmission and ``<tendon       │
│       damping>``                                                             │
│       for TENDON transmission. Ignored for SITE. (default: None)             │
│ --env.scene.entities.robot.articulation.actuators.0.delay-min-lag INT        │
│       Minimum command delay in physics timesteps.                            │
│                                                                              │
│                                                                              │
│       Each step, a lag is sampled uniformly from [min, max]. The command     │
│       target arrives                                                         │
│       that many steps late at the actuator's control law. Models             │
│       communication and bus                                                  │
│       latency between the policy and the motor (as opposed to observation    │
│       delay, which                                                           │
│       models sensor pipeline latency). (default: 0)                          │
│ --env.scene.entities.robot.articulation.actuators.0.delay-max-lag INT        │
│       Maximum command delay in physics timesteps. Set > 0 to enable delay.   │
│       (default: 0)                                                           │
│ --env.scene.entities.robot.articulation.actuators.0.delay-hold-prob FLOAT    │
│       Probability of keeping the current lag instead of resampling.          │
│       (default: 0.0)                                                         │
│ --env.scene.entities.robot.articulation.actuators.0.delay-update-period INT  │
│       How often to resample the lag, in physics timesteps (0 = every step).  │
│       (default: 0)                                                           │
│ --env.scene.entities.robot.articulation.actuators.0.delay-per-env-phase      │
│ {True,False}                                                                 │
│       Stagger lag resampling across environments so they don't all update    │
│       on the same step. (default: True)                                      │
│ --env.scene.entities.robot.articulation.actuators.0.command-field {None,     │
│ position,velocity,effort}                                                    │
│       If provided, require XML actuators to match this command field.        │
│       If None, auto-detect strictly from the spec element. (default:         │
│       position)                                                              │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.pedestal options ────────────────────────────────────────╮
│ --env.scene.entities.pedestal.spec-fn {fixed}                                │
│       (fixed to: <function pedestal_spec at 0x79307717bb00>)                 │
│ --env.scene.entities.pedestal.sort-actuators {True,False}                    │
│       When True, reorder actuators so that ``model.ctrl`` follows            │
│       joint/tendon/site                                                      │
│       definition order rather than the order actuators appear in the config. │
│       XML actuators                                                          │
│       are excluded from sorting and always retain their declaration order.   │
│       (default: False)                                                       │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.pedestal.init-state options ─────────────────────────────╮
│ --env.scene.entities.pedestal.init-state.pos (FLOAT,FLOAT,FLOAT)             │
│       Root position and orientation. (default: '(0.05, 0.15, 0.002)')        │
│ --env.scene.entities.pedestal.init-state.rot (FLOAT,FLOAT,FLOAT,FLOAT)       │
│       Root position and orientation. (default: '(1.0, 0.0, 0.0, 0.0)')       │
│ --env.scene.entities.pedestal.init-state.lin-vel (FLOAT,FLOAT,FLOAT)         │
│       Root linear and angular velocity (only for floating base entities).    │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.scene.entities.pedestal.init-state.ang-vel (FLOAT,FLOAT,FLOAT)         │
│       Root linear and angular velocity (only for floating base entities).    │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.scene.entities.pedestal.init-state.joint-pos {None,STR:FLOAT,...}      │
│       Articulation (only for articulated entities). Set to None to use the   │
│       model's existing keyframe (errors if none exists). (default:           │
│       '{'"'"'.*'"'"': 0.0}')                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.pedestal.init-state.joint-vel options ───────────────────╮
│ Articulation (only for articulated entities). Set to None to use the model's │
│ existing keyframe (errors if none exists).                                   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.scene.entities.pedestal.init-state.joint-vel.* FLOAT                   │
│       (default: 0.0)                                                         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.egg options ─────────────────────────────────────────────╮
│ --env.scene.entities.egg.spec-fn {fixed}                                     │
│       (fixed to: <function egg_spec at 0x79307717ba60>)                      │
│ --env.scene.entities.egg.sort-actuators {True,False}                         │
│       When True, reorder actuators so that ``model.ctrl`` follows            │
│       joint/tendon/site                                                      │
│       definition order rather than the order actuators appear in the config. │
│       XML actuators                                                          │
│       are excluded from sorting and always retain their declaration order.   │
│       (default: False)                                                       │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.egg.init-state options ──────────────────────────────────╮
│ --env.scene.entities.egg.init-state.pos (FLOAT,FLOAT,FLOAT)                  │
│       Root position and orientation. (default: '(0.05, 0.15, 0.098)')        │
│ --env.scene.entities.egg.init-state.rot (FLOAT,FLOAT,FLOAT,FLOAT)            │
│       Root position and orientation. (default: '(1.0, 0.0, 0.0, 0.0)')       │
│ --env.scene.entities.egg.init-state.lin-vel (FLOAT,FLOAT,FLOAT)              │
│       Root linear and angular velocity (only for floating base entities).    │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.scene.entities.egg.init-state.ang-vel (FLOAT,FLOAT,FLOAT)              │
│       Root linear and angular velocity (only for floating base entities).    │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.scene.entities.egg.init-state.joint-pos {None,STR:FLOAT,...}           │
│       Articulation (only for articulated entities). Set to None to use the   │
│       model's existing keyframe (errors if none exists). (default: '{}')     │
│ --env.scene.entities.egg.init-state.joint-vel {STR:FLOAT,...}                │
│       Articulation (only for articulated entities). Set to None to use the   │
│       model's existing keyframe (errors if none exists). (default: '{}')     │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.bucket options ──────────────────────────────────────────╮
│ --env.scene.entities.bucket.spec-fn {fixed}                                  │
│       (fixed to: <function bucket_spec at 0x79307717bba0>)                   │
│ --env.scene.entities.bucket.sort-actuators {True,False}                      │
│       When True, reorder actuators so that ``model.ctrl`` follows            │
│       joint/tendon/site                                                      │
│       definition order rather than the order actuators appear in the config. │
│       XML actuators                                                          │
│       are excluded from sorting and always retain their declaration order.   │
│       (default: False)                                                       │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.bucket.init-state options ───────────────────────────────╮
│ --env.scene.entities.bucket.init-state.pos (FLOAT,FLOAT,FLOAT)               │
│       Root position and orientation. (default: '(-0.05, 0.15, 0.0)')         │
│ --env.scene.entities.bucket.init-state.rot (FLOAT,FLOAT,FLOAT,FLOAT)         │
│       Root position and orientation. (default: '(1.0, 0.0, 0.0, 0.0)')       │
│ --env.scene.entities.bucket.init-state.lin-vel (FLOAT,FLOAT,FLOAT)           │
│       Root linear and angular velocity (only for floating base entities).    │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.scene.entities.bucket.init-state.ang-vel (FLOAT,FLOAT,FLOAT)           │
│       Root linear and angular velocity (only for floating base entities).    │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.scene.entities.bucket.init-state.joint-pos {None,STR:FLOAT,...}        │
│       Articulation (only for articulated entities). Set to None to use the   │
│       model's existing keyframe (errors if none exists). (default:           │
│       '{'"'"'.*'"'"': 0.0}')                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.bucket.init-state.joint-vel options ─────────────────────╮
│ Articulation (only for articulated entities). Set to None to use the model's │
│ existing keyframe (errors if none exists).                                   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.scene.entities.bucket.init-state.joint-vel.* FLOAT                     │
│       (default: 0.0)                                                         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor options ─────────────────────────────────────────────╮
│ Configuration for an observation group.                                      │
│                                                                              │
│ An observation group bundles multiple observation terms together. Groups are │
│ typically used to separate observations for different purposes (e.g.,        │
│ "actor" for the actor, "critic" for the value function).                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.concatenate-terms {True,False}                      │
│       Whether to concatenate all terms into a single tensor. If False,       │
│       returns                                                                │
│       a dict mapping term names to their individual tensors. (default: True) │
│ --env.observations.actor.concatenate-dim INT                                 │
│       Dimension along which to concatenate terms. Default -1 (last           │
│       dimension). (default: -1)                                              │
│ --env.observations.actor.enable-corruption {True,False}                      │
│       Whether to apply noise corruption to observations. Set to True during  │
│       training for domain randomization, False during evaluation. (default:  │
│       False)                                                                 │
│ --env.observations.actor.history-length {None}|INT                           │
│       Group-level history length override. If set, applies to all terms in   │
│       this group. If None, each term uses its own ``history_length``         │
│       setting. (default: None)                                               │
│ --env.observations.actor.flatten-history-dim {True,False}                    │
│       Whether to flatten history into the observation dimension. If True,    │
│       observations have shape ``(num_envs, obs_dim * history_length)``. If   │
│       False,                                                                 │
│       shape is ``(num_envs, history_length, obs_dim)``. (default: True)      │
│ --env.observations.actor.nan-policy {disabled,warn,sanitize,error}           │
│       NaN/Inf handling policy for observations in this group.                │
│                                                                              │
│                                                                              │
│       - 'disabled': No checks (default, fastest)                             │
│       - 'warn': Log warning with term name and env IDs, then sanitize        │
│       (debugging)                                                            │
│       - 'sanitize': Silent sanitization to 0.0 like reward manager (safe for │
│       production)                                                            │
│       - 'error': Raise ValueError on NaN/Inf (strict development mode)       │
│       (default: disabled)                                                    │
│ --env.observations.actor.nan-check-per-term {True,False}                     │
│       If True, check each observation term individually to identify NaN      │
│       source.                                                                │
│       If False, check only the final concatenated output (faster but less    │
│       informative).                                                          │
│       Only applies when nan_policy != 'disabled'. (default: True)            │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.tendon-len options ────────────────────────────╮
│ Configuration for an observation term.                                       │
│                                                                              │
│ Processing pipeline: compute → noise → clip → scale → delay → history. Delay │
│ models sensor latency. History provides temporal context. Both are optional  │
│ and can be combined.                                                         │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.tendon-len.func {fixed}                       │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function tendon_length at 0x79307717af20>)                        │
│ --env.observations.actor.terms.tendon-len.clip {None}|(FLOAT,FLOAT)          │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.actor.terms.tendon-len.scale {fixed}                      │
│       Scaling factor(s) to multiply the observation by. (fixed to: None)     │
│ --env.observations.actor.terms.tendon-len.delay-min-lag INT                  │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.tendon-len.delay-max-lag INT                  │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.actor.terms.tendon-len.delay-per-env {True,False}         │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.actor.terms.tendon-len.delay-hold-prob FLOAT              │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.actor.terms.tendon-len.delay-update-period INT            │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.actor.terms.tendon-len.delay-per-env-phase {True,False}   │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.actor.terms.tendon-len.history-length INT                 │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.tendon-len.flatten-history-dim {True,False}   │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.tendon-len.params.asset-cfg options ───────────╮
│ Configuration for a scene entity that is used by the manager's term.         │
│                                                                              │
│ This configuration allows flexible specification of entity components either │
│ by name or by ID. During resolution, it ensures consistency between names    │
│ and IDs, and can optimize to slice(None) when all components are selected.   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.name STR          │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.joint-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.joint-ids {fixed} │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.body-names        │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.body-ids {fixed}  │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.geom-names        │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.geom-ids {fixed}  │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.site-names        │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.site-ids {fixed}  │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.actuator-names    │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.actuator-ids      │
│ {fixed}                                                                      │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.tendon-names      │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: '('"'"'cable_0'"'"', '"'"'cable_1'"'"')')                    │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.tendon-ids        │
│ {fixed}                                                                      │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.camera-names      │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.camera-ids        │
│ {fixed}                                                                      │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.light-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.light-ids {fixed} │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.material-names    │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.material-ids      │
│ {fixed}                                                                      │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.pair-names        │
│ {None}|STR|(STR,...)                                                         │
│       Names of contact pairs to include. Can be a single string or tuple.    │
│       (default: None)                                                        │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.pair-ids {fixed}  │
│       IDs of contact pairs to include. Can be a list or slice. (fixed to:    │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-len.params.asset-cfg.preserve-order    │
│ {True,False}                                                                 │
│       If True, maintains the order of components as specified. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.tendon-vel options ────────────────────────────╮
│ Configuration for an observation term.                                       │
│                                                                              │
│ Processing pipeline: compute → noise → clip → scale → delay → history. Delay │
│ models sensor latency. History provides temporal context. Both are optional  │
│ and can be combined.                                                         │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.tendon-vel.func {fixed}                       │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function tendon_velocity at 0x79307717afc0>)                      │
│ --env.observations.actor.terms.tendon-vel.clip {None}|(FLOAT,FLOAT)          │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.actor.terms.tendon-vel.scale {fixed}                      │
│       Scaling factor(s) to multiply the observation by. (fixed to: None)     │
│ --env.observations.actor.terms.tendon-vel.delay-min-lag INT                  │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.tendon-vel.delay-max-lag INT                  │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.actor.terms.tendon-vel.delay-per-env {True,False}         │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.actor.terms.tendon-vel.delay-hold-prob FLOAT              │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.actor.terms.tendon-vel.delay-update-period INT            │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.actor.terms.tendon-vel.delay-per-env-phase {True,False}   │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.actor.terms.tendon-vel.history-length INT                 │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.tendon-vel.flatten-history-dim {True,False}   │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.tendon-vel.params.asset-cfg options ───────────╮
│ Configuration for a scene entity that is used by the manager's term.         │
│                                                                              │
│ This configuration allows flexible specification of entity components either │
│ by name or by ID. During resolution, it ensures consistency between names    │
│ and IDs, and can optimize to slice(None) when all components are selected.   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.name STR          │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.joint-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.joint-ids {fixed} │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.body-names        │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.body-ids {fixed}  │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.geom-names        │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.geom-ids {fixed}  │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.site-names        │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.site-ids {fixed}  │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.actuator-names    │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.actuator-ids      │
│ {fixed}                                                                      │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.tendon-names      │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: '('"'"'cable_0'"'"', '"'"'cable_1'"'"')')                    │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.tendon-ids        │
│ {fixed}                                                                      │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.camera-names      │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.camera-ids        │
│ {fixed}                                                                      │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.light-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.light-ids {fixed} │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.material-names    │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.material-ids      │
│ {fixed}                                                                      │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.pair-names        │
│ {None}|STR|(STR,...)                                                         │
│       Names of contact pairs to include. Can be a single string or tuple.    │
│       (default: None)                                                        │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.pair-ids {fixed}  │
│       IDs of contact pairs to include. Can be a list or slice. (fixed to:    │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.tendon-vel.params.asset-cfg.preserve-order    │
│ {True,False}                                                                 │
│       If True, maintains the order of components as specified. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.egg-to-bucket options ─────────────────────────╮
│ Configuration for an observation term.                                       │
│                                                                              │
│ Processing pipeline: compute → noise → clip → scale → delay → history. Delay │
│ models sensor latency. History provides temporal context. Both are optional  │
│ and can be combined.                                                         │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.egg-to-bucket.func {fixed}                    │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function egg_to_bucket at 0x79307717b1a0>)                        │
│ --env.observations.actor.terms.egg-to-bucket.clip {None}|(FLOAT,FLOAT)       │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.actor.terms.egg-to-bucket.scale {fixed}                   │
│       Scaling factor(s) to multiply the observation by. (fixed to: None)     │
│ --env.observations.actor.terms.egg-to-bucket.delay-min-lag INT               │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.egg-to-bucket.delay-max-lag INT               │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.actor.terms.egg-to-bucket.delay-per-env {True,False}      │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.actor.terms.egg-to-bucket.delay-hold-prob FLOAT           │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.actor.terms.egg-to-bucket.delay-update-period INT         │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.actor.terms.egg-to-bucket.delay-per-env-phase {True,      │
│ False}                                                                       │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.actor.terms.egg-to-bucket.history-length INT              │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.egg-to-bucket.flatten-history-dim {True,      │
│ False}                                                                       │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.egg-to-bucket.params.asset-cfg options ────────╮
│ Configuration for a scene entity that is used by the manager's term.         │
│                                                                              │
│ This configuration allows flexible specification of entity components either │
│ by name or by ID. During resolution, it ensures consistency between names    │
│ and IDs, and can optimize to slice(None) when all components are selected.   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.name STR       │
│       The name of the entity in the scene. (default: bucket)                 │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.joint-names    │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.joint-ids      │
│ {fixed}                                                                      │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.body-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.body-ids       │
│ {fixed}                                                                      │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.geom-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.geom-ids       │
│ {fixed}                                                                      │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.site-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       '('"'"'bucket_site'"'"',)')                                            │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.site-ids       │
│ {fixed}                                                                      │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.actuator-names │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.actuator-ids   │
│ {fixed}                                                                      │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.tendon-names   │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.tendon-ids     │
│ {fixed}                                                                      │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.camera-names   │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.camera-ids     │
│ {fixed}                                                                      │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.light-names    │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.light-ids      │
│ {fixed}                                                                      │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.material-names │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.material-ids   │
│ {fixed}                                                                      │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.pair-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of contact pairs to include. Can be a single string or tuple.    │
│       (default: None)                                                        │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.pair-ids       │
│ {fixed}                                                                      │
│       IDs of contact pairs to include. Can be a list or slice. (fixed to:    │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.egg-to-bucket.params.asset-cfg.preserve-order │
│ {True,False}                                                                 │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.last-action options ───────────────────────────╮
│ Configuration for an observation term.                                       │
│                                                                              │
│ Processing pipeline: compute → noise → clip → scale → delay → history. Delay │
│ models sensor latency. History provides temporal context. Both are optional  │
│ and can be combined.                                                         │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.last-action.func {fixed}                      │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function last_action at 0x79307717b2e0>)                          │
│ --env.observations.actor.terms.last-action.clip {None}|(FLOAT,FLOAT)         │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.actor.terms.last-action.scale {fixed}                     │
│       Scaling factor(s) to multiply the observation by. (fixed to: None)     │
│ --env.observations.actor.terms.last-action.delay-min-lag INT                 │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.last-action.delay-max-lag INT                 │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.actor.terms.last-action.delay-per-env {True,False}        │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.actor.terms.last-action.delay-hold-prob FLOAT             │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.actor.terms.last-action.delay-update-period INT           │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.actor.terms.last-action.delay-per-env-phase {True,False}  │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.actor.terms.last-action.history-length INT                │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.last-action.flatten-history-dim {True,False}  │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.robot-keypoints-xy options ────────────────────╮
│ Configuration for an observation term.                                       │
│                                                                              │
│ Processing pipeline: compute → noise → clip → scale → delay → history. Delay │
│ models sensor latency. History provides temporal context. Both are optional  │
│ and can be combined.                                                         │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.robot-keypoints-xy.func {fixed}               │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function robot_keypoints_xy at 0x79307717b880>)                   │
│ --env.observations.actor.terms.robot-keypoints-xy.clip {None}|(FLOAT,FLOAT)  │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.actor.terms.robot-keypoints-xy.scale {fixed}              │
│       Scaling factor(s) to multiply the observation by. (fixed to: None)     │
│ --env.observations.actor.terms.robot-keypoints-xy.delay-min-lag INT          │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.robot-keypoints-xy.delay-max-lag INT          │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.actor.terms.robot-keypoints-xy.delay-per-env {True,False} │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.actor.terms.robot-keypoints-xy.delay-hold-prob FLOAT      │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.actor.terms.robot-keypoints-xy.delay-update-period INT    │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.actor.terms.robot-keypoints-xy.delay-per-env-phase {True, │
│ False}                                                                       │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.actor.terms.robot-keypoints-xy.history-length INT         │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.robot-keypoints-xy.flatten-history-dim {True, │
│ False}                                                                       │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg options ───╮
│ Configuration for a scene entity that is used by the manager's term.         │
│                                                                              │
│ This configuration allows flexible specification of entity components either │
│ by name or by ID. During resolution, it ensures consistency between names    │
│ and IDs, and can optimize to slice(None) when all components are selected.   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.name STR  │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.joint-nam │
│ es {None}|STR|(STR,...)                                                      │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.joint-ids │
│ {fixed}                                                                      │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.body-name │
│ s {None}|STR|(STR,...)                                                       │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       '('"'"'link_001'"'"', '"'"'link_002'"'"', '"'"'link_003'"'"',          │
│       '"'"'link_004'"'"', '"'"'link_005'"'"', '"'"'link_006'"'"',            │
│       '"'"'link_007'"'"', '"'"'link_008'"'"', '"'"'link_009'"'"',            │
│       '"'"'link_010'"'"', '"'"'link_011'"'"', '"'"'link_012'"'"',            │
│       '"'"'link_013'"'"', '"'"'link_014'"'"', '"'"'link_015'"'"',            │
│       '"'"'link_016'"'"', '"'"'link_017'"'"', '"'"'link_018'"'"',            │
│       '"'"'link_019'"'"', '"'"'link_020'"'"', '"'"'link_021'"'"')')          │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.body-ids  │
│ {fixed}                                                                      │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.geom-name │
│ s {None}|STR|(STR,...)                                                       │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.geom-ids  │
│ {fixed}                                                                      │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.site-name │
│ s {None}|STR|(STR,...)                                                       │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.site-ids  │
│ {fixed}                                                                      │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.actuator- │
│ names {None}|STR|[STR,...]                                                   │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.actuator- │
│ ids {fixed}                                                                  │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.tendon-na │
│ mes {None}|STR|(STR,...)                                                     │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.tendon-id │
│ s {fixed}                                                                    │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.camera-na │
│ mes {None}|STR|(STR,...)                                                     │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.camera-id │
│ s {fixed}                                                                    │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.light-nam │
│ es {None}|STR|(STR,...)                                                      │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.light-ids │
│ {fixed}                                                                      │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.material- │
│ names {None}|STR|(STR,...)                                                   │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.material- │
│ ids {fixed}                                                                  │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.pair-name │
│ s {None}|STR|(STR,...)                                                       │
│       Names of contact pairs to include. Can be a single string or tuple.    │
│       (default: None)                                                        │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.pair-ids  │
│ {fixed}                                                                      │
│       IDs of contact pairs to include. Can be a list or slice. (fixed to:    │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.robot-keypoints-xy.params.asset-cfg.preserve- │
│ order {True,False}                                                           │
│       If True, maintains the order of components as specified. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.touch options ─────────────────────────────────╮
│ Configuration for an observation term.                                       │
│                                                                              │
│ Processing pipeline: compute → noise → clip → scale → delay → history. Delay │
│ models sensor latency. History provides temporal context. Both are optional  │
│ and can be combined.                                                         │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.touch.func {fixed}                            │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function touch_values at 0x79307717b240>)                         │
│ --env.observations.actor.terms.touch.clip {None}|(FLOAT,FLOAT)               │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.actor.terms.touch.scale {fixed}                           │
│       Scaling factor(s) to multiply the observation by. (fixed to: None)     │
│ --env.observations.actor.terms.touch.delay-min-lag INT                       │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.touch.delay-max-lag INT                       │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.actor.terms.touch.delay-per-env {True,False}              │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.actor.terms.touch.delay-hold-prob FLOAT                   │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.actor.terms.touch.delay-update-period INT                 │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.actor.terms.touch.delay-per-env-phase {True,False}        │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.actor.terms.touch.history-length INT                      │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.touch.flatten-history-dim {True,False}        │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic options ────────────────────────────────────────────╮
│ Configuration for an observation group.                                      │
│                                                                              │
│ An observation group bundles multiple observation terms together. Groups are │
│ typically used to separate observations for different purposes (e.g.,        │
│ "actor" for the actor, "critic" for the value function).                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.concatenate-terms {True,False}                     │
│       Whether to concatenate all terms into a single tensor. If False,       │
│       returns                                                                │
│       a dict mapping term names to their individual tensors. (default: True) │
│ --env.observations.critic.concatenate-dim INT                                │
│       Dimension along which to concatenate terms. Default -1 (last           │
│       dimension). (default: -1)                                              │
│ --env.observations.critic.enable-corruption {True,False}                     │
│       Whether to apply noise corruption to observations. Set to True during  │
│       training for domain randomization, False during evaluation. (default:  │
│       False)                                                                 │
│ --env.observations.critic.history-length {None}|INT                          │
│       Group-level history length override. If set, applies to all terms in   │
│       this group. If None, each term uses its own ``history_length``         │
│       setting. (default: None)                                               │
│ --env.observations.critic.flatten-history-dim {True,False}                   │
│       Whether to flatten history into the observation dimension. If True,    │
│       observations have shape ``(num_envs, obs_dim * history_length)``. If   │
│       False,                                                                 │
│       shape is ``(num_envs, history_length, obs_dim)``. (default: True)      │
│ --env.observations.critic.nan-policy {disabled,warn,sanitize,error}          │
│       NaN/Inf handling policy for observations in this group.                │
│                                                                              │
│                                                                              │
│       - 'disabled': No checks (default, fastest)                             │
│       - 'warn': Log warning with term name and env IDs, then sanitize        │
│       (debugging)                                                            │
│       - 'sanitize': Silent sanitization to 0.0 like reward manager (safe for │
│       production)                                                            │
│       - 'error': Raise ValueError on NaN/Inf (strict development mode)       │
│       (default: disabled)                                                    │
│ --env.observations.critic.nan-check-per-term {True,False}                    │
│       If True, check each observation term individually to identify NaN      │
│       source.                                                                │
│       If False, check only the final concatenated output (faster but less    │
│       informative).                                                          │
│       Only applies when nan_policy != 'disabled'. (default: True)            │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.tendon-len options ───────────────────────────╮
│ Configuration for an observation term.                                       │
│                                                                              │
│ Processing pipeline: compute → noise → clip → scale → delay → history. Delay │
│ models sensor latency. History provides temporal context. Both are optional  │
│ and can be combined.                                                         │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.tendon-len.func {fixed}                      │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function tendon_length at 0x79307717af20>)                        │
│ --env.observations.critic.terms.tendon-len.clip {None}|(FLOAT,FLOAT)         │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.tendon-len.scale {fixed}                     │
│       Scaling factor(s) to multiply the observation by. (fixed to: None)     │
│ --env.observations.critic.terms.tendon-len.delay-min-lag INT                 │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.tendon-len.delay-max-lag INT                 │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.tendon-len.delay-per-env {True,False}        │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.tendon-len.delay-hold-prob FLOAT             │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.tendon-len.delay-update-period INT           │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.tendon-len.delay-per-env-phase {True,False}  │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.tendon-len.history-length INT                │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.tendon-len.flatten-history-dim {True,False}  │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.tendon-len.params.asset-cfg options ──────────╮
│ Configuration for a scene entity that is used by the manager's term.         │
│                                                                              │
│ This configuration allows flexible specification of entity components either │
│ by name or by ID. During resolution, it ensures consistency between names    │
│ and IDs, and can optimize to slice(None) when all components are selected.   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.name STR         │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.joint-names      │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.joint-ids        │
│ {fixed}                                                                      │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.body-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.body-ids {fixed} │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.geom-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.geom-ids {fixed} │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.site-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.site-ids {fixed} │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.actuator-names   │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.actuator-ids     │
│ {fixed}                                                                      │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.tendon-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: '('"'"'cable_0'"'"', '"'"'cable_1'"'"')')                    │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.tendon-ids       │
│ {fixed}                                                                      │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.camera-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.camera-ids       │
│ {fixed}                                                                      │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.light-names      │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.light-ids        │
│ {fixed}                                                                      │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.material-names   │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.material-ids     │
│ {fixed}                                                                      │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.pair-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of contact pairs to include. Can be a single string or tuple.    │
│       (default: None)                                                        │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.pair-ids {fixed} │
│       IDs of contact pairs to include. Can be a list or slice. (fixed to:    │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-len.params.asset-cfg.preserve-order   │
│ {True,False}                                                                 │
│       If True, maintains the order of components as specified. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.tendon-vel options ───────────────────────────╮
│ Configuration for an observation term.                                       │
│                                                                              │
│ Processing pipeline: compute → noise → clip → scale → delay → history. Delay │
│ models sensor latency. History provides temporal context. Both are optional  │
│ and can be combined.                                                         │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.tendon-vel.func {fixed}                      │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function tendon_velocity at 0x79307717afc0>)                      │
│ --env.observations.critic.terms.tendon-vel.clip {None}|(FLOAT,FLOAT)         │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.tendon-vel.scale {fixed}                     │
│       Scaling factor(s) to multiply the observation by. (fixed to: None)     │
│ --env.observations.critic.terms.tendon-vel.delay-min-lag INT                 │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.tendon-vel.delay-max-lag INT                 │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.tendon-vel.delay-per-env {True,False}        │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.tendon-vel.delay-hold-prob FLOAT             │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.tendon-vel.delay-update-period INT           │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.tendon-vel.delay-per-env-phase {True,False}  │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.tendon-vel.history-length INT                │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.tendon-vel.flatten-history-dim {True,False}  │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.tendon-vel.params.asset-cfg options ──────────╮
│ Configuration for a scene entity that is used by the manager's term.         │
│                                                                              │
│ This configuration allows flexible specification of entity components either │
│ by name or by ID. During resolution, it ensures consistency between names    │
│ and IDs, and can optimize to slice(None) when all components are selected.   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.name STR         │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.joint-names      │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.joint-ids        │
│ {fixed}                                                                      │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.body-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.body-ids {fixed} │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.geom-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.geom-ids {fixed} │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.site-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.site-ids {fixed} │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.actuator-names   │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.actuator-ids     │
│ {fixed}                                                                      │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.tendon-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: '('"'"'cable_0'"'"', '"'"'cable_1'"'"')')                    │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.tendon-ids       │
│ {fixed}                                                                      │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.camera-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.camera-ids       │
│ {fixed}                                                                      │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.light-names      │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.light-ids        │
│ {fixed}                                                                      │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.material-names   │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.material-ids     │
│ {fixed}                                                                      │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.pair-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of contact pairs to include. Can be a single string or tuple.    │
│       (default: None)                                                        │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.pair-ids {fixed} │
│       IDs of contact pairs to include. Can be a list or slice. (fixed to:    │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.tendon-vel.params.asset-cfg.preserve-order   │
│ {True,False}                                                                 │
│       If True, maintains the order of components as specified. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.egg-to-bucket options ────────────────────────╮
│ Configuration for an observation term.                                       │
│                                                                              │
│ Processing pipeline: compute → noise → clip → scale → delay → history. Delay │
│ models sensor latency. History provides temporal context. Both are optional  │
│ and can be combined.                                                         │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.egg-to-bucket.func {fixed}                   │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function egg_to_bucket at 0x79307717b1a0>)                        │
│ --env.observations.critic.terms.egg-to-bucket.clip {None}|(FLOAT,FLOAT)      │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.egg-to-bucket.scale {fixed}                  │
│       Scaling factor(s) to multiply the observation by. (fixed to: None)     │
│ --env.observations.critic.terms.egg-to-bucket.delay-min-lag INT              │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.egg-to-bucket.delay-max-lag INT              │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.egg-to-bucket.delay-per-env {True,False}     │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.egg-to-bucket.delay-hold-prob FLOAT          │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.egg-to-bucket.delay-update-period INT        │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.egg-to-bucket.delay-per-env-phase {True,     │
│ False}                                                                       │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.egg-to-bucket.history-length INT             │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.egg-to-bucket.flatten-history-dim {True,     │
│ False}                                                                       │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.egg-to-bucket.params.asset-cfg options ───────╮
│ Configuration for a scene entity that is used by the manager's term.         │
│                                                                              │
│ This configuration allows flexible specification of entity components either │
│ by name or by ID. During resolution, it ensures consistency between names    │
│ and IDs, and can optimize to slice(None) when all components are selected.   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.name STR      │
│       The name of the entity in the scene. (default: bucket)                 │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.joint-names   │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.joint-ids     │
│ {fixed}                                                                      │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.body-names    │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.body-ids      │
│ {fixed}                                                                      │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.geom-names    │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.geom-ids      │
│ {fixed}                                                                      │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.site-names    │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       '('"'"'bucket_site'"'"',)')                                            │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.site-ids      │
│ {fixed}                                                                      │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.actuator-name │
│ s {None}|STR|[STR,...]                                                       │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.actuator-ids  │
│ {fixed}                                                                      │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.tendon-names  │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.tendon-ids    │
│ {fixed}                                                                      │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.camera-names  │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.camera-ids    │
│ {fixed}                                                                      │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.light-names   │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.light-ids     │
│ {fixed}                                                                      │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.material-name │
│ s {None}|STR|(STR,...)                                                       │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.material-ids  │
│ {fixed}                                                                      │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.pair-names    │
│ {None}|STR|(STR,...)                                                         │
│       Names of contact pairs to include. Can be a single string or tuple.    │
│       (default: None)                                                        │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.pair-ids      │
│ {fixed}                                                                      │
│       IDs of contact pairs to include. Can be a list or slice. (fixed to:    │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.egg-to-bucket.params.asset-cfg.preserve-orde │
│ r {True,False}                                                               │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.last-action options ──────────────────────────╮
│ Configuration for an observation term.                                       │
│                                                                              │
│ Processing pipeline: compute → noise → clip → scale → delay → history. Delay │
│ models sensor latency. History provides temporal context. Both are optional  │
│ and can be combined.                                                         │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.last-action.func {fixed}                     │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function last_action at 0x79307717b2e0>)                          │
│ --env.observations.critic.terms.last-action.clip {None}|(FLOAT,FLOAT)        │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.last-action.scale {fixed}                    │
│       Scaling factor(s) to multiply the observation by. (fixed to: None)     │
│ --env.observations.critic.terms.last-action.delay-min-lag INT                │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.last-action.delay-max-lag INT                │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.last-action.delay-per-env {True,False}       │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.last-action.delay-hold-prob FLOAT            │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.last-action.delay-update-period INT          │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.last-action.delay-per-env-phase {True,False} │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.last-action.history-length INT               │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.last-action.flatten-history-dim {True,False} │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.robot-keypoints-xy options ───────────────────╮
│ Configuration for an observation term.                                       │
│                                                                              │
│ Processing pipeline: compute → noise → clip → scale → delay → history. Delay │
│ models sensor latency. History provides temporal context. Both are optional  │
│ and can be combined.                                                         │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.robot-keypoints-xy.func {fixed}              │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function robot_keypoints_xy at 0x79307717b880>)                   │
│ --env.observations.critic.terms.robot-keypoints-xy.clip {None}|(FLOAT,FLOAT) │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.robot-keypoints-xy.scale {fixed}             │
│       Scaling factor(s) to multiply the observation by. (fixed to: None)     │
│ --env.observations.critic.terms.robot-keypoints-xy.delay-min-lag INT         │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.robot-keypoints-xy.delay-max-lag INT         │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.robot-keypoints-xy.delay-per-env {True,      │
│ False}                                                                       │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.robot-keypoints-xy.delay-hold-prob FLOAT     │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.robot-keypoints-xy.delay-update-period INT   │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.robot-keypoints-xy.delay-per-env-phase       │
│ {True,False}                                                                 │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.robot-keypoints-xy.history-length INT        │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.robot-keypoints-xy.flatten-history-dim       │
│ {True,False}                                                                 │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg options ──╮
│ Configuration for a scene entity that is used by the manager's term.         │
│                                                                              │
│ This configuration allows flexible specification of entity components either │
│ by name or by ID. During resolution, it ensures consistency between names    │
│ and IDs, and can optimize to slice(None) when all components are selected.   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.name STR │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.joint-na │
│ mes {None}|STR|(STR,...)                                                     │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.joint-id │
│ s {fixed}                                                                    │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.body-nam │
│ es {None}|STR|(STR,...)                                                      │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       '('"'"'link_001'"'"', '"'"'link_002'"'"', '"'"'link_003'"'"',          │
│       '"'"'link_004'"'"', '"'"'link_005'"'"', '"'"'link_006'"'"',            │
│       '"'"'link_007'"'"', '"'"'link_008'"'"', '"'"'link_009'"'"',            │
│       '"'"'link_010'"'"', '"'"'link_011'"'"', '"'"'link_012'"'"',            │
│       '"'"'link_013'"'"', '"'"'link_014'"'"', '"'"'link_015'"'"',            │
│       '"'"'link_016'"'"', '"'"'link_017'"'"', '"'"'link_018'"'"',            │
│       '"'"'link_019'"'"', '"'"'link_020'"'"', '"'"'link_021'"'"')')          │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.body-ids │
│ {fixed}                                                                      │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.geom-nam │
│ es {None}|STR|(STR,...)                                                      │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.geom-ids │
│ {fixed}                                                                      │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.site-nam │
│ es {None}|STR|(STR,...)                                                      │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.site-ids │
│ {fixed}                                                                      │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.actuator │
│ -names {None}|STR|[STR,...]                                                  │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.actuator │
│ -ids {fixed}                                                                 │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.tendon-n │
│ ames {None}|STR|(STR,...)                                                    │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.tendon-i │
│ ds {fixed}                                                                   │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.camera-n │
│ ames {None}|STR|(STR,...)                                                    │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.camera-i │
│ ds {fixed}                                                                   │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.light-na │
│ mes {None}|STR|(STR,...)                                                     │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.light-id │
│ s {fixed}                                                                    │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.material │
│ -names {None}|STR|(STR,...)                                                  │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.material │
│ -ids {fixed}                                                                 │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.pair-nam │
│ es {None}|STR|(STR,...)                                                      │
│       Names of contact pairs to include. Can be a single string or tuple.    │
│       (default: None)                                                        │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.pair-ids │
│ {fixed}                                                                      │
│       IDs of contact pairs to include. Can be a list or slice. (fixed to:    │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.robot-keypoints-xy.params.asset-cfg.preserve │
│ -order {True,False}                                                          │
│       If True, maintains the order of components as specified. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.touch options ────────────────────────────────╮
│ Configuration for an observation term.                                       │
│                                                                              │
│ Processing pipeline: compute → noise → clip → scale → delay → history. Delay │
│ models sensor latency. History provides temporal context. Both are optional  │
│ and can be combined.                                                         │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.touch.func {fixed}                           │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function touch_values at 0x79307717b240>)                         │
│ --env.observations.critic.terms.touch.clip {None}|(FLOAT,FLOAT)              │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.touch.scale {fixed}                          │
│       Scaling factor(s) to multiply the observation by. (fixed to: None)     │
│ --env.observations.critic.terms.touch.delay-min-lag INT                      │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.touch.delay-max-lag INT                      │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.touch.delay-per-env {True,False}             │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.touch.delay-hold-prob FLOAT                  │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.touch.delay-update-period INT                │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.touch.delay-per-env-phase {True,False}       │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.touch.history-length INT                     │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.touch.flatten-history-dim {True,False}       │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.actions.cable-len options ──────────────────────────────────────────────╮
│ Tendon-length action with command-rate limiting.                             │
│                                                                              │
│ The inherited TendonLengthActionCfg first converts raw policy actions into   │
│ desired tendon-length targets using:                                         │
│                                                                              │
│                                                                              │
│     desired = raw_action * scale + offset                                    │
│                                                                              │
│ and then applies any configured clip range. This subclass preserves that     │
│ full target range, but limits how far the actually commanded target may move │
│ per policy/control step.                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.actions.cable-len.entity-name STR                                      │
│       Name of the entity in the scene that this action term controls.        │
│       (default: robot)                                                       │
│ --env.actions.cable-len.clip {None,STR:TUPLE,...}                            │
│       Optional clipping bounds applied to processed actions (after scale     │
│       and offset). Dict maps actuator name regex patterns to (min, max)      │
│       tuples, resolved the same way as ``scale`` and ``offset``. (default:   │
│       '{'"'"'cable_0'"'"': (0.15, 0.29), '"'"'cable_1'"'"': (0.15, 0.29)}')  │
│ --env.actions.cable-len.transmission-type {JOINT,TENDON,SITE}                │
│       Type of transmission to control. (default: TENDON)                     │
│ --env.actions.cable-len.actuator-names (STR,...)|[STR,...]                   │
│       Actuator names to control. (default: '('"'"'cable_0'"'"',              │
│       '"'"'cable_1'"'"')')                                                   │
│ --env.actions.cable-len.scale FLOAT|{STR:FLOAT,...}                          │
│       Action scale. Float or dict mapping actuator names to scales.          │
│       (default: 0.06999999999999999)                                         │
│ --env.actions.cable-len.offset FLOAT|{STR:FLOAT,...}                         │
│       Action offset. Float or dict mapping actuator names to offsets.        │
│       (default: 0.22)                                                        │
│ --env.actions.cable-len.preserve-order {True,False}                          │
│       Whether to preserve the order of actuator names. (default: True)       │
│ --env.actions.cable-len.max-delta-per-step FLOAT                             │
│       Maximum tendon-length command change per policy/control step, in       │
│       metres.                                                                │
│                                                                              │
│                                                                              │
│       Example: if the effective control timestep is 0.01 s, then 1.5e-4      │
│       m/step is                                                              │
│       0.015 m/s. A target jump from 0.22 to 0.25 m therefore takes about 2   │
│       s. (default: 0.001)                                                    │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.reset-scene-to-default options ──────────────────────────────────╮
│ Configuration for an event term.                                             │
│                                                                              │
│ Event terms trigger operations at specific simulation events. They're        │
│ commonly used for domain randomization, state resets, and periodic           │
│ perturbations.                                                               │
│                                                                              │
│ The four modes determine when the event fires:                               │
│                                                                              │
│                                                                              │
│ - ``"startup"``: Once when the environment initializes. Use for parameters   │
│ that                                                                         │
│   should be randomized per-environment but stay constant within an episode   │
│ (e.g.,                                                                       │
│   domain randomization).                                                     │
│                                                                              │
│                                                                              │
│ - ``"reset"``: On every episode reset. Use for parameters that should vary   │
│ between                                                                      │
│   episodes (e.g., initial robot pose, domain randomization).                 │
│                                                                              │
│                                                                              │
│ - ``"interval"``: Periodically during simulation, controlled by              │
│ ``interval_range_s``.                                                        │
│   Use for perturbations that should happen during episodes (e.g., pushing    │
│ the robot,                                                                   │
│   external disturbances).                                                    │
│                                                                              │
│                                                                              │
│ - ``"step"``: Every environment step, unconditionally on all envs. Use for   │
│ terms that                                                                   │
│   manage per-step state such as force lifetimes (e.g.,                       │
│ ``apply_body_impulse``).                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.reset-scene-to-default.func {fixed}                             │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function reset_scene_to_default at 0x793087f7e480>)               │
│ --env.events.reset-scene-to-default.mode {startup,reset,interval,step}       │
│       When the event triggers: ``"startup"`` (once at init), ``"reset"``     │
│       (every                                                                 │
│       episode), ``"interval"`` (periodically during simulation), or          │
│       ``"step"`` (every                                                      │
│       environment step). (default: reset)                                    │
│ --env.events.reset-scene-to-default.interval-range-s {None}|(FLOAT,FLOAT)    │
│       Time range in seconds for interval mode. The next trigger time is      │
│       uniformly                                                              │
│       sampled from ``[min, max]``. Required when ``mode="interval"``.        │
│       (default: None)                                                        │
│ --env.events.reset-scene-to-default.is-global-time {True,False}              │
│       Whether all environments share the same timer. If True, all envs       │
│       trigger                                                                │
│       simultaneously. If False (default), each env has an independent timer  │
│       that                                                                   │
│       resets on episode reset. Only applies to ``mode="interval"``.          │
│       (default: False)                                                       │
│ --env.events.reset-scene-to-default.min-step-count-between-reset INT         │
│       Minimum environment steps between triggers. Prevents the event from    │
│       firing                                                                 │
│       too frequently when episodes reset rapidly. Only applies to            │
│       ``mode="reset"``.                                                      │
│       Set to 0 (default) to trigger on every reset. (default: 0)             │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.sim options ────────────────────────────────────────────────────────────╮
│ Simulation configuration including physics timestep, solver iterations,      │
│ contact parameters, and NaN guarding.                                        │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.sim.nconmax {None}|INT                                                 │
│       Number of contacts to allocate per world.                              │
│                                                                              │
│                                                                              │
│       Contacts exist in large heterogenous arrays: one world may have more   │
│       than nconmax                                                           │
│       contacts. If None, a heuristic value is used. (default: 512)           │
│ --env.sim.njmax {None}|INT                                                   │
│       Number of constraints to allocate per world.                           │
│                                                                              │
│                                                                              │
│       Constraint arrays are batched by world: no world may have more than    │
│       njmax                                                                  │
│       constraints. If None, a heuristic value is used. (default: 1400)       │
│ --env.sim.ls-parallel {True,False}                                           │
│       Boosts perf quite noticeably. (default: True)                          │
│ --env.sim.contact-sensor-maxmatch INT                                        │
│       (default: 256)                                                         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.sim.mujoco options ─────────────────────────────────────────────────────╮
│ --env.sim.mujoco.timestep FLOAT                                              │
│       Integrator settings. (default: 0.0001)                                 │
│ --env.sim.mujoco.integrator {euler,implicitfast}                             │
│       Integrator settings. (default: implicitfast)                           │
│ --env.sim.mujoco.impratio FLOAT                                              │
│       Friction settings. (default: 10)                                       │
│ --env.sim.mujoco.cone {pyramidal,elliptic}                                   │
│       Friction settings. (default: elliptic)                                 │
│ --env.sim.mujoco.jacobian {auto,dense,sparse}                                │
│       Solver settings. (default: auto)                                       │
│ --env.sim.mujoco.solver {newton,cg,pgs}                                      │
│       Solver settings. (default: newton)                                     │
│ --env.sim.mujoco.iterations INT                                              │
│       Solver settings. (default: 50)                                         │
│ --env.sim.mujoco.tolerance FLOAT                                             │
│       Solver settings. (default: 1e-08)                                      │
│ --env.sim.mujoco.ls-iterations INT                                           │
│       Solver settings. (default: 50)                                         │
│ --env.sim.mujoco.ls-tolerance FLOAT                                          │
│       Solver settings. (default: 0.01)                                       │
│ --env.sim.mujoco.ccd-iterations INT                                          │
│       Solver settings. (default: 50)                                         │
│ --env.sim.mujoco.gravity (FLOAT,FLOAT,FLOAT)                                 │
│       Other. (default: '(0.0, 0.0, -9.81)')                                  │
│ --env.sim.mujoco.disableflags (STR,...)                                      │
│       Disable flags to set (e.g. ``("contact",)`` to disable contacts).      │
│       (default: '()')                                                        │
│ --env.sim.mujoco.enableflags (STR,...)                                       │
│       Enable flags to set (e.g. ``("energy",)`` to enable energy             │
│       computation). (default: '()')                                          │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.sim.nan-guard options ──────────────────────────────────────────────────╮
│ --env.sim.nan-guard.enabled {True,False}                                     │
│       (default: False)                                                       │
│ --env.sim.nan-guard.buffer-size INT                                          │
│       (default: 100)                                                         │
│ --env.sim.nan-guard.output-dir STR                                           │
│       (default: /tmp/mjlab/nan_dumps)                                        │
│ --env.sim.nan-guard.max-envs-to-dump INT                                     │
│       (default: 5)                                                           │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.viewer options ─────────────────────────────────────────────────────────╮
│ Viewer configuration for rendering (camera position, resolution, etc.).      │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.viewer.lookat (FLOAT,FLOAT,FLOAT)                                      │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.viewer.distance FLOAT                                                  │
│       (default: 0.75)                                                        │
│ --env.viewer.fovy {None}|FLOAT                                               │
│       (default: None)                                                        │
│ --env.viewer.elevation FLOAT                                                 │
│       (default: -20.0)                                                       │
│ --env.viewer.azimuth FLOAT                                                   │
│       (default: 120.0)                                                       │
│ --env.viewer.origin-type {AUTO,WORLD,ASSET_ROOT,ASSET_BODY}                  │
│       (default: ASSET_BODY)                                                  │
│ --env.viewer.entity-name {None}|STR                                          │
│       (default: robot)                                                       │
│ --env.viewer.body-name {None}|STR                                            │
│       (default: link_010)                                                    │
│ --env.viewer.env-idx INT                                                     │
│       (default: 0)                                                           │
│ --env.viewer.max-extra-envs INT                                              │
│       Number of neighboring environments to render around ``env_idx``.       │
│       (default: 2)                                                           │
│ --env.viewer.enable-reflections {True,False}                                 │
│       (default: True)                                                        │
│ --env.viewer.enable-shadows {True,False}                                     │
│       (default: True)                                                        │
│ --env.viewer.height INT                                                      │
│       (default: 240)                                                         │
│ --env.viewer.width INT                                                       │
│       (default: 320)                                                         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.egg-delta-progress options ─────────────────────────────────────╮
│ Configuration for a reward term.                                             │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.egg-delta-progress.func {fixed}                                │
│       The callable that computes this reward term's value. (fixed to:        │
│       <function egg_to_bucket_delta_progress at 0x79307717b7e0>)             │
│ --env.rewards.egg-delta-progress.weight FLOAT                                │
│       Weight multiplier for this reward term. (default: 1.0)                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.egg-delta-progress.params options ──────────────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.egg-delta-progress.params.progress-scale FLOAT                 │
│       (default: 0.005)                                                       │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.egg-delta-progress.params.asset-cfg options ────────────────────╮
│ Configuration for a scene entity that is used by the manager's term.         │
│                                                                              │
│ This configuration allows flexible specification of entity components either │
│ by name or by ID. During resolution, it ensures consistency between names    │
│ and IDs, and can optimize to slice(None) when all components are selected.   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.egg-delta-progress.params.asset-cfg.name STR                   │
│       The name of the entity in the scene. (default: bucket)                 │
│ --env.rewards.egg-delta-progress.params.asset-cfg.joint-names                │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.rewards.egg-delta-progress.params.asset-cfg.joint-ids {fixed}          │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.rewards.egg-delta-progress.params.asset-cfg.body-names                 │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.rewards.egg-delta-progress.params.asset-cfg.body-ids {fixed}           │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.rewards.egg-delta-progress.params.asset-cfg.geom-names                 │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.rewards.egg-delta-progress.params.asset-cfg.geom-ids {fixed}           │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.rewards.egg-delta-progress.params.asset-cfg.site-names                 │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       '('"'"'bucket_site'"'"',)')                                            │
│ --env.rewards.egg-delta-progress.params.asset-cfg.site-ids {fixed}           │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.rewards.egg-delta-progress.params.asset-cfg.actuator-names             │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.rewards.egg-delta-progress.params.asset-cfg.actuator-ids {fixed}       │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.rewards.egg-delta-progress.params.asset-cfg.tendon-names               │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.rewards.egg-delta-progress.params.asset-cfg.tendon-ids {fixed}         │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.rewards.egg-delta-progress.params.asset-cfg.camera-names               │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.rewards.egg-delta-progress.params.asset-cfg.camera-ids {fixed}         │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.rewards.egg-delta-progress.params.asset-cfg.light-names                │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.rewards.egg-delta-progress.params.asset-cfg.light-ids {fixed}          │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.rewards.egg-delta-progress.params.asset-cfg.material-names             │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.rewards.egg-delta-progress.params.asset-cfg.material-ids {fixed}       │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.rewards.egg-delta-progress.params.asset-cfg.pair-names                 │
│ {None}|STR|(STR,...)                                                         │
│       Names of contact pairs to include. Can be a single string or tuple.    │
│       (default: None)                                                        │
│ --env.rewards.egg-delta-progress.params.asset-cfg.pair-ids {fixed}           │
│       IDs of contact pairs to include. Can be a list or slice. (fixed to:    │
│       slice(None, None, None))                                               │
│ --env.rewards.egg-delta-progress.params.asset-cfg.preserve-order {True,      │
│ False}                                                                       │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.inside-bucket options ──────────────────────────────────────────╮
│ Configuration for a reward term.                                             │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.inside-bucket.func {fixed}                                     │
│       The callable that computes this reward term's value. (fixed to:        │
│       <function egg_inside_bucket_terminal_indicator at 0x79307717b420>)     │
│ --env.rewards.inside-bucket.weight FLOAT                                     │
│       Weight multiplier for this reward term. (default: 25.0)                │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.inside-bucket.params options ───────────────────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.inside-bucket.params.com-distance-threshold FLOAT              │
│       (default: 0.015)                                                       │
│ --env.rewards.inside-bucket.params.max-z-offset FLOAT                        │
│       (default: 0.02)                                                        │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.inside-bucket.params.asset-cfg options ─────────────────────────╮
│ Configuration for a scene entity that is used by the manager's term.         │
│                                                                              │
│ This configuration allows flexible specification of entity components either │
│ by name or by ID. During resolution, it ensures consistency between names    │
│ and IDs, and can optimize to slice(None) when all components are selected.   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.inside-bucket.params.asset-cfg.name STR                        │
│       The name of the entity in the scene. (default: bucket)                 │
│ --env.rewards.inside-bucket.params.asset-cfg.joint-names {None}|STR|(STR,    │
│ ...)                                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.rewards.inside-bucket.params.asset-cfg.joint-ids {fixed}               │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.rewards.inside-bucket.params.asset-cfg.body-names {None}|STR|(STR,...) │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.rewards.inside-bucket.params.asset-cfg.body-ids {fixed}                │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.rewards.inside-bucket.params.asset-cfg.geom-names {None}|STR|(STR,...) │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.rewards.inside-bucket.params.asset-cfg.geom-ids {fixed}                │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.rewards.inside-bucket.params.asset-cfg.site-names {None}|STR|(STR,...) │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       '('"'"'bucket_site'"'"',)')                                            │
│ --env.rewards.inside-bucket.params.asset-cfg.site-ids {fixed}                │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.rewards.inside-bucket.params.asset-cfg.actuator-names {None}|STR|[STR, │
│ ...]                                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.rewards.inside-bucket.params.asset-cfg.actuator-ids {fixed}            │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.rewards.inside-bucket.params.asset-cfg.tendon-names {None}|STR|(STR,   │
│ ...)                                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.rewards.inside-bucket.params.asset-cfg.tendon-ids {fixed}              │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.rewards.inside-bucket.params.asset-cfg.camera-names {None}|STR|(STR,   │
│ ...)                                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.rewards.inside-bucket.params.asset-cfg.camera-ids {fixed}              │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.rewards.inside-bucket.params.asset-cfg.light-names {None}|STR|(STR,    │
│ ...)                                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.rewards.inside-bucket.params.asset-cfg.light-ids {fixed}               │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.rewards.inside-bucket.params.asset-cfg.material-names {None}|STR|(STR, │
│ ...)                                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.rewards.inside-bucket.params.asset-cfg.material-ids {fixed}            │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.rewards.inside-bucket.params.asset-cfg.pair-names {None}|STR|(STR,...) │
│       Names of contact pairs to include. Can be a single string or tuple.    │
│       (default: None)                                                        │
│ --env.rewards.inside-bucket.params.asset-cfg.pair-ids {fixed}                │
│       IDs of contact pairs to include. Can be a list or slice. (fixed to:    │
│       slice(None, None, None))                                               │
│ --env.rewards.inside-bucket.params.asset-cfg.preserve-order {True,False}     │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.egg-fell-penalty options ───────────────────────────────────────╮
│ Configuration for a reward term.                                             │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.egg-fell-penalty.func {fixed}                                  │
│       The callable that computes this reward term's value. (fixed to:        │
│       <function egg_fell_terminal_indicator at 0x79307717b560>)              │
│ --env.rewards.egg-fell-penalty.weight FLOAT                                  │
│       Weight multiplier for this reward term. (default: -10.0)               │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.terminations.success-egg-inside-bucket options ─────────────────────────╮
│ Configuration for a termination term.                                        │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.terminations.success-egg-inside-bucket.func {fixed}                    │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function egg_inside_bucket at 0x79307717b380>)                    │
│ --env.terminations.success-egg-inside-bucket.time-out {True,False}           │
│       Whether the term contributes towards episodic timeouts. (default:      │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.terminations.success-egg-inside-bucket.params options ──────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.terminations.success-egg-inside-bucket.params.com-distance-threshold   │
│ FLOAT                                                                        │
│       (default: 0.015)                                                       │
│ --env.terminations.success-egg-inside-bucket.params.max-z-offset FLOAT       │
│       (default: 0.02)                                                        │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.terminations.success-egg-inside-bucket.params.asset-cfg options ────────╮
│ Configuration for a scene entity that is used by the manager's term.         │
│                                                                              │
│ This configuration allows flexible specification of entity components either │
│ by name or by ID. During resolution, it ensures consistency between names    │
│ and IDs, and can optimize to slice(None) when all components are selected.   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.name STR       │
│       The name of the entity in the scene. (default: bucket)                 │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.joint-names    │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.joint-ids      │
│ {fixed}                                                                      │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.body-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.body-ids       │
│ {fixed}                                                                      │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.geom-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.geom-ids       │
│ {fixed}                                                                      │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.site-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       '('"'"'bucket_site'"'"',)')                                            │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.site-ids       │
│ {fixed}                                                                      │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.actuator-names │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.actuator-ids   │
│ {fixed}                                                                      │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.tendon-names   │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.tendon-ids     │
│ {fixed}                                                                      │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.camera-names   │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.camera-ids     │
│ {fixed}                                                                      │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.light-names    │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.light-ids      │
│ {fixed}                                                                      │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.material-names │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.material-ids   │
│ {fixed}                                                                      │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.pair-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of contact pairs to include. Can be a single string or tuple.    │
│       (default: None)                                                        │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.pair-ids       │
│ {fixed}                                                                      │
│       IDs of contact pairs to include. Can be a list or slice. (fixed to:    │
│       slice(None, None, None))                                               │
│ --env.terminations.success-egg-inside-bucket.params.asset-cfg.preserve-order │
│ {True,False}                                                                 │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.terminations.egg-fell options ──────────────────────────────────────────╮
│ Configuration for a termination term.                                        │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.terminations.egg-fell.func {fixed}                                     │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function egg_fell at 0x79307717b4c0>)                             │
│ --env.terminations.egg-fell.time-out {True,False}                            │
│       Whether the term contributes towards episodic timeouts. (default:      │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.terminations.egg-oob options ───────────────────────────────────────────╮
│ Configuration for a termination term.                                        │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.terminations.egg-oob.func {fixed}                                      │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function egg_out_of_bounds at 0x79307717b6a0>)                    │
│ --env.terminations.egg-oob.time-out {True,False}                             │
│       Whether the term contributes towards episodic timeouts. (default:      │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.terminations.nan-state options ─────────────────────────────────────────╮
│ Configuration for a termination term.                                        │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.terminations.nan-state.func {fixed}                                    │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function nan_state at 0x79307717b740>)                            │
│ --env.terminations.nan-state.time-out {True,False}                           │
│       Whether the term contributes towards episodic timeouts. (default:      │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.terminations.time-out options ──────────────────────────────────────────╮
│ Configuration for a termination term.                                        │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.terminations.time-out.func {fixed}                                     │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function time_out at 0x793087f7ff60>)                             │
│ --env.terminations.time-out.time-out {True,False}                            │
│       Whether the term contributes towards episodic timeouts. (default:      │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ agent options ──────────────────────────────────────────────────────────────╮
│ --agent.seed INT                                                             │
│       The seed for the experiment. Default is 42. (default: 42)              │
│ --agent.num-steps-per-env INT                                                │
│       The number of steps per environment update. (default: 32)              │
│ --agent.max-iterations INT                                                   │
│       The maximum number of iterations. (default: 1000)                      │
│ --agent.save-interval INT                                                    │
│       The number of iterations between saves. (default: 50)                  │
│ --agent.experiment-name STR                                                  │
│       Directory name used to group runs under                                │
│       ``{log_root}/{experiment_name}/``.                                     │
│       The log root defaults to ``logs/rsl_rl`` and can be overridden with    │
│       ``--log-root`` on the CLI. (default: spirob_minimal)                   │
│ --agent.run-name STR                                                         │
│       Optional label appended to the timestamped run directory               │
│       (e.g. ``2025-01-27_14-30-00_{run_name}``). Also becomes the            │
│       display name for the run in wandb. (default: '')                       │
│ --agent.logger {wandb,tensorboard}                                           │
│       The logger to use. Default is wandb. (default: wandb)                  │
│ --agent.wandb-project STR                                                    │
│       The wandb project name. (default: mjlab)                               │
│ --agent.wandb-tags (STR,...)                                                 │
│       Tags for the wandb run. Default is empty tuple. (default: '()')        │
│ --agent.resume {True,False}                                                  │
│       Whether to resume the experiment. Default is False. (default: False)   │
│ --agent.load-run STR                                                         │
│       The run directory to load. Default is ".*" which means all runs. If    │
│       regex                                                                  │
│       expression, the latest (alphabetical order) matching run will be       │
│       loaded. (default: '.*')                                                │
│ --agent.load-checkpoint STR                                                  │
│       The checkpoint file to load. Default is "model_.*.pt" (all). If regex  │
│       expression,                                                            │
│       the latest (alphabetical order) matching file will be loaded.          │
│       (default: 'model_.*.pt')                                               │
│ --agent.clip-actions {None}|FLOAT                                            │
│       The clipping range for action values. If None (default), no clipping   │
│       is applied. (default: None)                                            │
│ --agent.upload-model {True,False}                                            │
│       Whether to upload model files (.pt, .onnx) to W&B on save. Set to      │
│       False to keep metric logging but avoid storage usage. Default is True. │
│       (default: True)                                                        │
│ --agent.class-name STR                                                       │
│       The runner class name. Default is OnPolicyRunner. (default:            │
│       OnPolicyRunner)                                                        │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ agent.obs-groups options ───────────────────────────────────────────────────╮
│ --agent.obs-groups.actor (STR,...)                                           │
│       (default: '('"'"'actor'"'"',)')                                        │
│ --agent.obs-groups.critic (STR,...)                                          │
│       (default: '('"'"'critic'"'"',)')                                       │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ agent.actor options ────────────────────────────────────────────────────────╮
│ The actor configuration.                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --agent.actor.hidden-dims (INT,...)                                          │
│       The hidden dimensions of the network. (default: '(128, 128)')          │
│ --agent.actor.activation STR                                                 │
│       The activation function. (default: elu)                                │
│ --agent.actor.obs-normalization {True,False}                                 │
│       Whether to normalize the observations. Default is False. (default:     │
│       True)                                                                  │
│ --agent.actor.rnn-type {None}|STR                                            │
│       RNN type ("lstm" or "gru"). When set, class_name should be "RNNModel". │
│       (default: None)                                                        │
│ --agent.actor.rnn-hidden-dim INT                                             │
│       Hidden state dimension for the RNN. (default: 256)                     │
│ --agent.actor.rnn-num-layers INT                                             │
│       Number of stacked RNN layers. (default: 1)                             │
│ --agent.actor.class-name STR                                                 │
│       Model class name resolved by RSL-RL (MLPModel, CNNModel, or RNNModel). │
│       (default: MLPModel)                                                    │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ agent.actor.distribution-cfg options ───────────────────────────────────────╮
│ --agent.actor.distribution-cfg.class-name STR                                │
│       (default: GaussianDistribution)                                        │
│ --agent.actor.distribution-cfg.init-std FLOAT                                │
│       (default: 1.0)                                                         │
│ --agent.actor.distribution-cfg.std-type STR                                  │
│       (default: scalar)                                                      │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ agent.critic options ───────────────────────────────────────────────────────╮
│ The critic configuration.                                                    │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --agent.critic.hidden-dims (INT,...)                                         │
│       The hidden dimensions of the network. (default: '(128, 128)')          │
│ --agent.critic.activation STR                                                │
│       The activation function. (default: elu)                                │
│ --agent.critic.obs-normalization {True,False}                                │
│       Whether to normalize the observations. Default is False. (default:     │
│       True)                                                                  │
│ --agent.critic.rnn-type {None}|STR                                           │
│       RNN type ("lstm" or "gru"). When set, class_name should be "RNNModel". │
│       (default: None)                                                        │
│ --agent.critic.rnn-hidden-dim INT                                            │
│       Hidden state dimension for the RNN. (default: 256)                     │
│ --agent.critic.rnn-num-layers INT                                            │
│       Number of stacked RNN layers. (default: 1)                             │
│ --agent.critic.class-name STR                                                │
│       Model class name resolved by RSL-RL (MLPModel, CNNModel, or RNNModel). │
│       (default: MLPModel)                                                    │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ agent.algorithm options ────────────────────────────────────────────────────╮
│ The algorithm configuration.                                                 │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --agent.algorithm.num-learning-epochs INT                                    │
│       The number of learning epochs per update. (default: 5)                 │
│ --agent.algorithm.num-mini-batches INT                                       │
│       The number of mini-batches per update.                                 │
│       mini batch size = num_envs * num_steps / num_mini_batches (default: 4) │
│ --agent.algorithm.learning-rate FLOAT                                        │
│       The learning rate. (default: 0.0003)                                   │
│ --agent.algorithm.schedule {adaptive,fixed}                                  │
│       The learning rate schedule. (default: adaptive)                        │
│ --agent.algorithm.gamma FLOAT                                                │
│       The discount factor. (default: 0.99)                                   │
│ --agent.algorithm.lam FLOAT                                                  │
│       The lambda parameter for Generalized Advantage Estimation (GAE).       │
│       (default: 0.95)                                                        │
│ --agent.algorithm.entropy-coef FLOAT                                         │
│       The coefficient for the entropy loss. (default: 0.01)                  │
│ --agent.algorithm.desired-kl FLOAT                                           │
│       The desired KL divergence between the new and old policies. (default:  │
│       0.01)                                                                  │
│ --agent.algorithm.max-grad-norm FLOAT                                        │
│       The maximum gradient norm for the policy. (default: 1.0)               │
│ --agent.algorithm.value-loss-coef FLOAT                                      │
│       The coefficient for the value loss. (default: 1.0)                     │
│ --agent.algorithm.use-clipped-value-loss {True,False}                        │
│       Whether to use clipped value loss. (default: True)                     │
│ --agent.algorithm.clip-param FLOAT                                           │
│       The clipping parameter for the policy. (default: 0.2)                  │
│ --agent.algorithm.normalize-advantage-per-mini-batch {True,False}            │
│       Whether to normalize the advantage per mini-batch. Default is False.   │
│       If True, the                                                           │
│       advantage is normalized over the mini-batches only. Otherwise, the     │
│       advantage is                                                           │
│       normalized over the entire collected trajectories. (default: False)    │
│ --agent.algorithm.optimizer {adam,adamw,sgd,rmsprop}                         │
│       The optimizer to use. (default: adam)                                  │
│ --agent.algorithm.share-cnn-encoders {True,False}                            │
│       Share CNN encoders between actor and critic. (default: False)          │
│ --agent.algorithm.class-name STR                                             │
│       Algorithm class name resolved by RSL-RL. (default: PPO)                │
╰──────────────────────────────────────────────────────────────────────────────╯
